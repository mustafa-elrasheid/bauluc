
function fibonacci(let int n) => int:
	let int x = 0;
	let int y = 1;
	let int i = 0;
	while n > i:
		let int temp = x+y;
		x = y;
		y = temp;
		i = i + 1;

	return y;

function sum_primes(let int n):
	let int total = 0;
	let int i = 0;
	while i < n:
		let int is_prime = 1;
		let int ii = 2;
		while ii < i:
			if i % ii == 0:
				is_prime = 0;
			ii = ii + 1;
		if is_prime:
			total = total+i;
		i=i+1;
	return total;

function main() => int:
	let int result = sum_primes(200);
	return result;